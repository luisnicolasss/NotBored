package com.example.notbored

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import android.view.MenuItem
import android.view.View
import com.example.notbored.databinding.ActivityActivitiesViewBinding
import com.example.notbored.databinding.ActivitySuggestItemBinding
import java.util.Observer

class Suggest_item : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        val binding = ActivitySuggestItemBinding.inflate(layoutInflater)

        super.onCreate(savedInstanceState)
        setContentView(binding.root)







}}